services:
  - type: web
    name: aurea
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
